# This service was previously used to trigger a local script for capturing faces.
# This approach is not suitable for a web API.
# The functionality to save images is now handled directly in the
# /usuarios/<id_usuario>/capturas endpoint in controllers/usuario_controller.py